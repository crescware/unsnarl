//! Binary crate library surface. Exposes CLI types for integration
//! testing and (eventually) pipeline functions for in-process invocation
//! from the workspace-root parity harness (`<root>/tests/parity.rs`).

pub mod cli;
